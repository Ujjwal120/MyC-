#include<bits/stdc++.h>
#define fast ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0);

using namespace std;

int n;

void solve() {
    int x, y; cin >> x >> y;
    cout << x-1 << " " << y << "\n";
}

signed main() {
    fast int t = 1;
    cin >> t;
    while(t--) solve();
    return 0;
}